import HomeScreen from './HomeScreen';
import * as SCREEN_IDS from './ids';

const homeScreens = {
  homeScreen: {
    id: SCREEN_IDS.HOME,
    component: HomeScreen,
  },
};

export default homeScreens;
